# 50 - Daily

One note per day, ISO-named (`2026-05-28.md`), from the `daily.md` template.

This folder sits at the vault root, outside the Work/Private split - a single day usually mixes both. Provenance goes in the note's `context:` frontmatter field instead.
